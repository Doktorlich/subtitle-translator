import FormSettings from "./FormSettings.tsx";

export default function SectionFormSettings() {
    return (
        <section>
            <h2>Settings</h2>
            <FormSettings />
        </section>
    );
}
